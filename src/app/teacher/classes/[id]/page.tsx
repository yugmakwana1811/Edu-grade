import Link from "next/link";
import { ArrowLeft, BookOpen, Copy, FileText, Plus, Users } from "lucide-react";
import { notFound } from "next/navigation";
import { requireUser } from "@/lib/auth";
import { db } from "@/lib/db";
import { Alert, EmptyState, PageHeader, StatCard } from "@/components/ui";
import { formatDate } from "@/lib/utils";
export default async function ClassDetail({ params, searchParams }: { params: Promise<{ id: string }>; searchParams: Promise<{ success?: string }> }) { const [{ id }, { success }] = await Promise.all([params, searchParams]); const user = await requireUser("TEACHER"); const c = await db.classRoom.findFirst({ where: { id, teacherId: user.teacherProfile!.id }, include: { enrollments: { include: { student: { include: { user: true } } } }, assignments: { orderBy: { dueAt: "asc" }, take: 6 }, announcements: { orderBy: { publishedAt: "desc" }, take: 3 }, resources: { orderBy: { createdAt: "desc" }, take: 3 } } }); if (!c) notFound(); return <div className="page"><Link href="/teacher/classes" className="hint" style={{ display: "inline-flex", gap: ".4rem", alignItems: "center", marginBottom: "1rem" }}><ArrowLeft size={15}/> All classes</Link><PageHeader eyebrow={`${c.subject} · Class ${c.grade}`} title={c.name} description={c.description ?? "Your connected class workspace."} action={<Link href={`/teacher/assignments/new?classId=${c.id}`} className="btn btn-primary"><Plus size={16}/> New assignment</Link>}/><Alert success={success}/><div className="grid-auto"><StatCard label="Enrolled students" value={c.enrollments.length} icon={Users}/><StatCard label="Assignments" value={c.assignments.length} icon={FileText} tone="coral"/><StatCard label="Resources" value={c.resources.length} icon={BookOpen}/><StatCard label="Class code" value={c.code} detail="Share with students" icon={Copy} tone="gold"/></div><div style={{ display: "grid", gridTemplateColumns: "1.2fr .8fr", gap: "1rem", marginTop: "1rem" }}><section className="card card-pad"><div className="eyebrow">Assigned work</div><h2 className="display" style={{ fontSize: "1.8rem", margin: ".3rem 0 1rem" }}>Class timeline</h2>{c.assignments.length ? c.assignments.map((a)=><Link key={a.id} href={`/teacher/assignments/${a.id}`} style={{ display: "flex", justifyContent: "space-between", alignItems: "center", padding: ".85rem 0", borderBottom: "1px solid var(--line)" }}><div><strong>{a.title}</strong><div className="hint">{a.type} · {a.maxMarks} marks</div></div><div style={{ textAlign: "right" }}><span className={`badge ${a.status === "PUBLISHED" ? "badge-teal" : ""}`}>{a.status.toLowerCase()}</span><div className="hint" style={{ marginTop: ".3rem" }}>{formatDate(a.dueAt)}</div></div></Link>) : <EmptyState title="No work assigned" description="Create a real assignment, test, worksheet or practice activity."/>}</section><aside style={{ display: "grid", gap: "1rem" }}><section className="card card-pad"><div className="eyebrow">Enrolled learners</div><h2 className="display" style={{ fontSize: "1.6rem", margin: ".3rem 0 1rem" }}>Class list</h2>{c.enrollments.length ? c.enrollments.map((e)=><div key={e.id} style={{ display: "flex", justifyContent: "space-between", padding: ".7rem 0", borderBottom: "1px solid var(--line)" }}><strong style={{ fontSize: ".85rem" }}>{e.student.user.name}</strong><span className="hint">{e.student.rollNumber}</span></div>) : <p className="hint">Share code <strong>{c.code}</strong> to enroll students.</p>}</section><section className="card card-pad"><div className="eyebrow">Latest notice</div>{c.announcements[0] ? <><h3>{c.announcements[0].title}</h3><p className="hint" style={{ lineHeight: 1.55 }}>{c.announcements[0].content}</p><Link href="/teacher/announcements" style={{ color: "var(--teal)", fontWeight: 800, fontSize: ".82rem" }}>Manage announcements</Link></> : <p className="hint">No announcements yet.</p>}</section></aside></div></div>; }
