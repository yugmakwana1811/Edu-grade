import Link from "next/link";
import { ArrowRight, CheckCircle2, Clock3, Inbox } from "lucide-react";
import { requireUser } from "@/lib/auth";
import { db } from "@/lib/db";
import { EmptyState, PageHeader, StatCard } from "@/components/ui";
import { formatDateTime } from "@/lib/utils";
export default async function ReviewQueue(){const user=await requireUser("TEACHER");const submissions=await db.submission.findMany({where:{assignment:{class:{teacherId:user.teacherProfile!.id}},status:{not:"DRAFT"}},include:{assignment:{include:{class:true}},student:{include:{user:true}},pages:true,result:true},orderBy:{submittedAt:"desc"}});const pending=submissions.filter(s=>s.status==="SUBMITTED").length;const checked=submissions.filter(s=>["REVIEWED","PUBLISHED"].includes(s.status)).length;return <div className="page"><PageHeader eyebrow="Submission review" title="Thoughtful feedback, faster" description="See handwritten pages, use AI for a starting suggestion, enter marks, edit feedback, and decide when the result is ready."/><div className="grid-auto" style={{marginBottom:"1rem"}}><StatCard label="Pending review" value={pending} icon={Clock3} tone="coral"/><StatCard label="Checked work" value={checked} icon={CheckCircle2}/><StatCard label="All received" value={submissions.length} icon={Inbox} tone="gold"/></div>{submissions.length?<div className="card table-wrap"><table><thead><tr><th>Student</th><th>Assignment</th><th>Pages</th><th>Submitted</th><th>Status</th><th>Score</th><th></th></tr></thead><tbody>{submissions.map(s=><tr key={s.id}><td><strong>{s.student.user.name}</strong><div className="hint">{s.student.rollNumber}</div></td><td>{s.assignment.title}<div className="hint">{s.assignment.class.name}</div></td><td>{s.pages.length}</td><td>{s.submittedAt?formatDateTime(s.submittedAt):"—"}</td><td><span className={`badge ${s.status==="SUBMITTED"?"badge-coral":"badge-teal"}`}>{s.status.toLowerCase()}</span></td><td>{s.result?`${Number(s.result.marks)}/${s.assignment.maxMarks}`:"—"}</td><td><Link href={`/teacher/review/${s.id}`} aria-label={`Review ${s.student.user.name}`}><ArrowRight size={17}/></Link></td></tr>)}</tbody></table></div>:<EmptyState title="The review queue is clear" description="Submitted student work will appear here automatically."/>}</div>}
