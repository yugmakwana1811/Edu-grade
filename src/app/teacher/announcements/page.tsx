import { Send } from "lucide-react";
import { requireUser } from "@/lib/auth";
import { db } from "@/lib/db";
import { Alert, EmptyState, PageHeader } from "@/components/ui";
import { SubmitButton } from "@/components/submit-button";
import { createAnnouncementAction } from "@/app/actions";
import { formatDateTime } from "@/lib/utils";
export default async function Announcements({searchParams}:{searchParams:Promise<{error?:string;success?:string}>}){const [{error,success},user]=await Promise.all([searchParams,requireUser("TEACHER")]);const [classes,list]=await Promise.all([db.classRoom.findMany({where:{teacherId:user.teacherProfile!.id}}),db.announcement.findMany({where:{class:{teacherId:user.teacherProfile!.id}},include:{class:true},orderBy:{publishedAt:"desc"}})]);return <div className="page"><PageHeader eyebrow="Class communication" title="Announcements and reminders" description="Keep every learner informed with clear messages tied to the right class."/><Alert error={error} success={success}/><div style={{display:"grid",gridTemplateColumns:"minmax(0,1.3fr) minmax(300px,.7fr)",gap:"1rem",alignItems:"start"}}><section>{list.length?<div style={{display:"grid",gap:".8rem"}}>{list.map(a=><article className="card card-pad" key={a.id}><div style={{display:"flex",justifyContent:"space-between",gap:"1rem"}}><span className="badge badge-teal">{a.class.name}</span><span className="hint">{formatDateTime(a.publishedAt)}</span></div><h2 className="display" style={{fontSize:"1.55rem",margin:".8rem 0 .35rem"}}>{a.title}</h2><p style={{color:"var(--muted)",lineHeight:1.6,margin:0}}>{a.content}</p></article>)}</div>:<EmptyState title="No announcements sent" description="Share a reminder, learning update, or classroom note."/>}</section><aside className="card card-pad"><div className="eyebrow">New message</div><h2 className="display" style={{fontSize:"1.7rem",margin:".3rem 0 1rem"}}>Send to a class</h2><form action={createAnnouncementAction} style={{display:"grid",gap:".8rem"}}><label><span className="label">Class</span><select className="field" name="classId" required><option value="">Select class</option>{classes.map(c=><option value={c.id} key={c.id}>{c.name}</option>)}</select></label><label><span className="label">Title</span><input className="field" name="title" required minLength={3} maxLength={100} placeholder="Revision clinic on Wednesday"/></label><label><span className="label">Message</span><textarea className="field" name="content" required minLength={5} maxLength={1000} placeholder="Share the essential details and any action students should take." style={{minHeight:180}}/></label><SubmitButton pendingText="Sending…" confirmMessage="Send this announcement now? Every enrolled student in the selected class will see it immediately."><Send size={16}/> Send announcement</SubmitButton><a href="/teacher/ai-tools" style={{textAlign:"center",color:"var(--teal)",fontSize:".8rem",fontWeight:800}}>Draft with AI studio instead</a></form></aside></div></div>}
