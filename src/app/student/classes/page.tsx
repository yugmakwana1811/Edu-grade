import Link from "next/link";
import { ArrowRight, KeyRound, School } from "lucide-react";
import { requireUser } from "@/lib/auth";
import { db } from "@/lib/db";
import { Alert, EmptyState, PageHeader } from "@/components/ui";
import { SubmitButton } from "@/components/submit-button";
import { joinClassAction } from "@/app/actions";
export default async function StudentClasses({searchParams}:{searchParams:Promise<{error?:string}>}){const [{error},user]=await Promise.all([searchParams,requireUser("STUDENT")]);const enrollments=await db.classEnrollment.findMany({where:{studentId:user.studentProfile!.id},include:{class:{include:{teacher:{include:{user:true}},_count:{select:{assignments:true,resources:true}}}}}});return <div className="page"><PageHeader eyebrow="My classes" title="Your learning spaces" description="Open a class for assigned work, resources, quizzes, and announcements."/><Alert error={error}/><div style={{display:"grid",gridTemplateColumns:"minmax(0,1.35fr) minmax(280px,.65fr)",gap:"1rem",alignItems:"start"}}><section>{enrollments.length?<div className="grid-auto">{enrollments.map(({class:c})=><Link href={`/student/classes/${c.id}`} className="card card-pad" key={c.id}><span className="badge badge-teal">Class {c.grade}</span><h2 className="display" style={{fontSize:"1.6rem",margin:".8rem 0 .2rem"}}>{c.name}</h2><p style={{color:"var(--muted)",margin:"0 0 1rem"}}>{c.subject} · {c.teacher.user.name}</p><div style={{display:"flex",justifyContent:"space-between",alignItems:"center",fontSize:".8rem"}}><span>{c._count.assignments} assignments · {c._count.resources} resources</span><ArrowRight size={17}/></div></Link>)}</div>:<EmptyState title="You have not joined a class" description="Ask your teacher for the six-character class code, then enter it here."/>}</section><aside className="card card-pad"><KeyRound color="var(--coral)"/><div className="eyebrow" style={{marginTop:".8rem"}}>Join a class</div><h2 className="display" style={{fontSize:"1.7rem",margin:".3rem 0 .7rem"}}>Enter your class code</h2><p className="hint">Codes contain six letters and numbers. The expo class code is <strong>ACC12D</strong>.</p><form action={joinClassAction} style={{display:"grid",gap:".7rem"}}><input className="field" name="code" required minLength={6} maxLength={6} pattern="[A-Za-z0-9]{6}" placeholder="ACC12D" style={{textTransform:"uppercase",letterSpacing:".18em",fontWeight:850,textAlign:"center"}}/><SubmitButton><School size={16}/> Join class</SubmitButton></form></aside></div></div>}
